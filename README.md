# Setup Docker on EC2 instance(Ubuntu)

# Tools 

- Terraform
- Ansible

# Run

- Requirement install terraform & ansible, config AWS IAM on local

```shell
terraform init

terraform apply -auto-approve
```
